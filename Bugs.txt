已知bug：
    作为顾客登陆时，如果点击登陆又取消 再次登陆时，显示的可用餐桌数量会叠加（显示错误）

    //  作为顾客登陆时，如果不是数据库中的账户，也会把用户名存入tables中

    //  新创建一个用户时，如果不进行登陆操作，似乎无法将新账户填入数据库中

    新创建一个用户后，再进行登录操作，所有窗口会自动关闭

    后台管理中，删除一个账户后，tableView不会刷新显示

    //  在editAccount中的comboBox中选择了隐藏某些行之后，没有设置自动恢复

    //  需要修改status所对应的身份等级：0 for customer；
                                    1 for waiter;
                                    2 for cook;
                                    3 for supervisor;
